<?xml version="1.0" encoding="utf-8" ?>
<odoo>
     <record id="view_recurring_contract_form" model="ir.ui.view">
        <field name="name">recurring.contract.form</field>
        <field name="model">recurring.contract</field>
        <field
      name="inherit_id"
      ref="sponsorship_compassion.view_sponsorship_contract_form_compassion"
    />
        <field name="arch" type="xml">
            <group name="sponsorship_details">
                <field name="send_introduction_letter" />
            </group>
        </field>
     </record>

    <record id="contract_filter" model="ir.ui.view">
        <field name="name">recurring.contract.search</field>
        <field name="model">recurring.contract</field>
        <field
      name="inherit_id"
      ref="sponsorship_compassion.view_recurring_contract_filter_graph"
    />
        <field name="arch" type="xml">
            <filter name="last_3_months" position="after">
                <separator />
                <filter
          name="new_picture"
          domain="[('new_picture', '=', True)]"
          string="New picture"
        />
            </filter>
        </field>
    </record>

    <!-- Flag SDS sub-management cards whose exit communication is still pending. -->
    <record id="view_follow_contract_kanban_exit_pending" model="ir.ui.view">
        <field name="name">follow.contract.kanban.exit.pending</field>
        <field name="model">recurring.contract</field>
        <field
      name="inherit_id"
      ref="sponsorship_sub_management.view_follow_contract_kanban_compassion"
    />
        <field name="arch" type="xml">
            <field name="sds_state" position="after">
                <field name="exit_communication_pending" />
            </field>
            <xpath expr="//footer" position="before">
                <div
          t-if="record.exit_communication_pending.raw_value"
          class="text-warning"
        >
                    <i
            class="fa fa-exclamation-triangle"
          /> Awaiting exit communication
                </div>
            </xpath>
        </field>
    </record>
</odoo>
