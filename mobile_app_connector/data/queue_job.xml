<odoo>
    <!-- Channels -->
    <record id="channel_mobile_app" model="queue.job.channel">
        <field name="name">mobile_app_connector</field>
        <field name="parent_id" ref="queue_job.channel_root"/>
    </record>

    <!-- Job functions -->
    <record id="remove_donation_job" model="queue.job.function">
        <field name="model_id" ref="model_account_invoice"/>
        <field name="method">remove_mobile_donation_if_not_paid</field>
        <field name="channel_id" ref="channel_mobile_app"/>
    </record>
</odoo>
