<?xml version="1.0" encoding="utf-8"?>
<odoo>
    <record id="view_invoice_no_notifications" model="ir.ui.view">
        <field name="name">account.invoice.attribution.form</field>
        <field name="model">account.invoice</field>
        <field name="inherit_id" ref="account.invoice_form" />
        <field name="arch" type="xml">
            <field name="returned_payment" position="after">
                <label for="no_notifications"/>
                <field name="no_notifications"/>
            </field>
        </field>
    </record>
</odoo>
