<?xml version="1.0" encoding="utf-8"?>
<openerp>
	<data>
        <record id="view_sponsorship_contract_tree_compassion" model="ir.ui.view">
			<field name="name">sponsorship.contract.compassion.tree</field>
			<field name="model">sponsorship.contract</field>
			<field name="arch" type="xml">
					<tree string="Sponsorships" colors="blue:state == 'draft';red:state == 'mandate';green:state == 'waiting';black:state == 'active';gray:state in ('terminated','cancelled')">
                        <field name="reference"/>
                        <field name="partner_codega"/>
                        <field name="partner_id"/>
                        <field name="child_code"/>
                        <field name="child_id"/>
                        <field name="start_date"/>
                        <field name="end_date"/>
                        <field name="payment_term_id"/>
                        <field name="frequency"/>
                        <field name="total_amount"/>
                        <field name="state"/>
					</tree>
			</field>
		</record>
		
		<record id="view_sponsorship_contract_form_compassion" model="ir.ui.view">
			<field name="name">sponsorship.contract.compassion.form</field>
			<field name="model">sponsorship.contract</field>
			<field name="arch" type="xml">
                <form string="Sponsorship" version="7.0">
                    <header>
                        <field name="state" widget="statusbar" statusbar_visible="draft,active,terminated" />
                        <button name="contract_validated" class="oe_highlight" string="Validate" states="draft" />
                        <button name="contract_terminated" class="oe_highlight" string="Terminate" states="active" />
                    </header>
                    <sheet>
                        <div class="oe_title">
                            <h1>
                                <field name="reference" default_focus="1" />
                            </h1>
                        </div>
                        <group>
                            <group>
                                <field name="partner_id" on_change="on_change_partner_id(partner_id)" />
                                <field name="group_id" domain="[('partner_id', '=', partner_id)]" context="{'default_partner_id': partner_id}" />
                            </group>
                            <group>
                                <field name="start_date" on_change="on_change_start_date(start_date)" />
                                <field name="end_date" />
                                <field name="next_invoice_date" />
                            </group>
                        </group>
                        <notebook>
                            <page string="Contract lines">
                                <field name="contract_line_ids" widget="one2many_list" />
                                <group class="oe_subtotal_footer oe_right">
                                    <field name="total_amount" />
                                </group>
                                <group class="oe_inline" />
                            </page>
                            <page string="Related invoice lines">
                                <field name="invoice_line_ids">
                                    <tree>
                                        <field name="invoice_id" />
                                        <field name="name" />
                                        <field name="due_date" />
                                        <field name="quantity" />
                                        <field name="price_unit" />
                                        <field name="price_subtotal" />
                                        <field name="state" />
                                    </tree>
                                </field>
                            </page>
                        </notebook>
                    </sheet>
                    <div class="oe_chatter">
                        <field name="message_follower_ids" widget="mail_followers"/>
                        <field name="message_ids" widget="mail_thread"/>
                    </div>
                </form>
            </field>
		</record>
        
        <record id="action_sponsorship_contract" model="ir.actions.act_window">
            <field name="name">Sponsorships</field>
            <field name="type">ir.actions.act_window</field>
            <field name="res_model">sponsorship.contract</field>
            <field name="view_type">form</field>
            <field name="view_mode">tree,form</field>
        </record>

        
        <!-- Move the Sponsorships Menu to the Sponsorship Section -->
        <menuitem id="menu_sponsorship_contract_form" parent="child_compassion.menu_sponsorship_section"
                  name="Sponsorships" sequence="1" action="action_sponsorship_contract"/>
        <menuitem id="contract_compassion.menu_contracts_section" parent="child_compassion.menu_sponsorship_root" 
                  name="Invoicing" groups="account.group_account_user" sequence="2"/>
        <menuitem id="open_customers" parent="child_compassion.menu_sponsorship_section"
                  name="Partners" action="base.action_partner_customer_form" sequence="3"/>
	</data>
</openerp>
